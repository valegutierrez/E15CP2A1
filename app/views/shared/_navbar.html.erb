<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= root_path %>">Pictory</a>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><a href="<%= histories_userhistories_path %>">My Histories</a></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
          <% if signed_in? %>
            <%= current_user.email %>
          <% else %>
            Ingreso
          <% end %>
          <span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <% if signed_in? %>
              <li><a class="dropdown-item" href="<%= edit_user_registration_path %>">Editar Usuario</a></li>
              <li><a class="dropdown-item" href="<%= destroy_user_session_path %>" data-method="DELETE">Cerrar Sesión</a></li>
            <% else %>
              <li><a class="dropdown-item" href="<%= new_user_registration_path %>">Crear Usuario</a></li>
              <li><a class="dropdown-item" href="<%= new_user_session_path %>">Iniciar Sesión</a></li>
            <% end %>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>

